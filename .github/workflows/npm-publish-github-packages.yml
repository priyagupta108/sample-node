# name: Issue 995 Workflow

# on:
#   push:
#     # branches:
#       - issue-995

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v4

#     - name: Set up Node.js
#       uses: actions/setup-node@v4
#       env:
#         NODE_AUTH_TOKEN: "MyAuthToken"
#       with:
#         registry-url: 'https://my-repo.example.com/npm-private/'
#         scope: 'myscope'

#     - name: Display .npmrc if exists
#       if: success()
#       run: cat $NPM_CONFIG_USERCONFIG
#     - name: Echo RUNNER_TEMP
#       run: echo ${RUNNER_TEMP}

#     - name: List files in RUNNER_TEMP
#       run: ls -al ${RUNNER_TEMP}

name: npm always-auth warning
on: push

jobs:
  repro:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Node.js
        uses: actions/setup-node@v6
        # uses: priyagupta108/setup-node@remove-always-auth-log
        with:
          node-version: '14'
          


      - name: Install the latest version of the npm CLI
        run: |
          npm install -g npm@latest

      # Run various npm commands to trigger the warning
      - run: npm --version
      - run: npm config ls

      - name: Inspect the config set up by setup-node
        run: |
          cat /home/runner/work/_temp/.npmrc
