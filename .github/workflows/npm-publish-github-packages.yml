name: Test

on:
  - pull_request
  - push

jobs:
  quality:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v5

      - uses: priyagupta108/setup-node@eslint-update
      # - uses: actions/setup-node@v4
        with:
          node-version: "22.x"

      

      - name: Install Dependencies
        run: npm ci

      # # - name: Prettier
      # #   run: npm run fmt:check

      # - name: ESlint
      #   run: npm run eslint:github-action
