name: Print Tcl/Tk Version

on: [push, pull_request]

jobs:
  print-tcl-version:
    runs-on: macos-13

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Print Tcl/Tk version
      run: |
        tclsh <<< 'puts [info patchlevel]'
        wish <<< 'puts [info patchlevel]'
